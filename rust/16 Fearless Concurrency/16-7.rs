use std::sync::mpsc;
// mpsc stands for multiple producer, single consumer!.

fn main() {
    let (tx, rx) = mpsc::channel();

    thread::spawn(move || {
        let val = String::from("hi");
        tx.send(val).unwrap();
    });
}

